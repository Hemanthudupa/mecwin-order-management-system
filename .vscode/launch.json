{
  "configurations": [
    {
      "name": "Launch_Server",
      "program": "main.js",
      "request": "launch",
      "skipFiles": ["<node_internals>/**"],
      "type": "node",
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}"
    }
  ]
}
